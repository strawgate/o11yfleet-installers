// Re-exported from `@o11yfleet/db` so existing relative imports
// (`../db/schema.js`) keep working. The canonical schema lives next to
// the SQL migrations in `packages/db/`.
export * from "@o11yfleet/db";
