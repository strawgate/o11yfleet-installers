name: Release Deploy

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      skip_tests:
        description: "Skip test jobs (deploy only)"
        required: false
        default: "false"
        type: boolean

concurrency:
  group: production-deploy
  cancel-in-progress: false

permissions:
  contents: read

env:
  PNPM_VERSION: 9.15.4
  NODE_VERSION: "22"

jobs:
  test:
    if: ${{ github.event.inputs.skip_tests != 'true' }}
    runs-on: ubicloud-standard-2-arm
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: pnpm/action-setup@26f6d4f2c533a43e6b5da0b4a5dd983f98f7b49a # v6.0.4
        with:
          version: ${{ env.PNPM_VERSION }}

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint & typecheck
        run: pnpm turbo lint typecheck

      - name: Run all tests
        run: pnpm turbo test

  deploy-production:
    needs: [test]
    if: ${{ always() && (needs.test.result == 'success' || needs.test.result == 'skipped') }}
    runs-on: ubicloud-standard-2-arm
    environment: production
    # Cloud-provider secrets (AWS_*, CLOUDFLARE_*, O11YFLEET_*) are
    # intentionally NOT in this job-wide env block. Job env is inherited
    # by every step including `pnpm install --frozen-lockfile` inside
    # setup-deploy, where a malicious lifecycle script in any transitive
    # dependency could exfiltrate them. Secrets are scoped at step level
    # on the steps that actually need them.
    env:
      TF_IN_AUTOMATION: "true"
      TF_INPUT: "false"
      TERRAFORM_STATE_R2_BUCKET: ${{ vars.TERRAFORM_STATE_R2_BUCKET }}
      TERRAFORM_STATE_R2_ENDPOINT: ${{ vars.TERRAFORM_STATE_R2_ENDPOINT }}
      TERRAFORM_STATE_R2_REGION: ${{ vars.TERRAFORM_STATE_R2_REGION || 'auto' }}
      TERRAFORM_PROVIDER_V5_STATE_READY: ${{ vars.TERRAFORM_PROVIDER_V5_STATE_READY }}
      TERRAFORM_PRODUCTION_APPLY_ENABLED: ${{ vars.TERRAFORM_PRODUCTION_APPLY_ENABLED }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - uses: ./.github/actions/setup-deploy
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Check Terraform remote state configuration
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.TERRAFORM_STATE_R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TERRAFORM_STATE_R2_SECRET_ACCESS_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_DEPLOY_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_DEPLOY_ACCOUNT_ID }}
        run: |
          test -n "$TERRAFORM_STATE_R2_BUCKET"
          test -n "$TERRAFORM_STATE_R2_ENDPOINT"
          test -n "$AWS_ACCESS_KEY_ID"
          test -n "$AWS_SECRET_ACCESS_KEY"
          test -n "$CLOUDFLARE_API_TOKEN"
          test -n "$CLOUDFLARE_ACCOUNT_ID"
          test "$TERRAFORM_PROVIDER_V5_STATE_READY" = "true"
          test "$TERRAFORM_PRODUCTION_APPLY_ENABLED" = "true"

      # Production D1 migrations are expected to be expand-only and backward
      # compatible with the currently deployed Worker. Contract/destructive
      # schema changes need a separate release after old code is gone.
      - name: Deploy production environment
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.TERRAFORM_STATE_R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.TERRAFORM_STATE_R2_SECRET_ACCESS_KEY }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_DEPLOY_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_DEPLOY_ACCOUNT_ID }}
        run: just deploy-env prod

      - name: Resolve production site smoke targets
        id: site-smoke
        run: |
          {
            echo 'targets<<TARGETS'
            just --quiet env-site-smoke-targets prod
            echo 'TARGETS'
          } >> "$GITHUB_OUTPUT"

      - name: Smoke test site targets
        uses: ./.github/actions/smoke-site
        with:
          smoke-targets: ${{ steps.site-smoke.outputs.targets }}

      - name: Smoke test production API
        uses: ./.github/actions/smoke-api
        with:
          environment: prod
          origin: https://o11yfleet.com
          config-name: ci-release-config
          token-label: ci-release
        env:
          O11YFLEET_API_BEARER_SECRET: ${{ secrets.O11YFLEET_API_BEARER_SECRET }}
          O11YFLEET_SEED_ADMIN_EMAIL: ${{ secrets.O11YFLEET_SEED_ADMIN_EMAIL }}
          O11YFLEET_SEED_ADMIN_PASSWORD: ${{ secrets.O11YFLEET_SEED_ADMIN_PASSWORD }}

      - name: Smoke test production public aliases
        run: just smoke-aliases-ci prod
