[package]
name = "o11y-opamp-proto"
version = "0.1.0"
edition = "2021"
description = "Shared OpAMP protobuf types for Rust and WASM"

[dependencies]
prost = "0.14"
serde = { version = "1", features = ["derive"] }

[build-dependencies]
prost-build = "0.14"
